import 'package:flutter/material.dart';

class Registe extends StatelessWidget {
  const Registe({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("hello"),
    );
  }
}
